# CNN_pms
A simple Convolutional Neural Network (CNN) implemented with PyTorch for classifying the MNIST handwritten digit dataset. Supports GPU acceleration (auto-detect CUDA availability), with complete training and validation pipelines, and real-time accuracy printing during training.
